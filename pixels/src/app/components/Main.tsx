// import Login from './Login'
import Banner from './Banner';
const Main = async () => {
  // const session = await getServerAuthSession();
  // console.log(session);
  return (
      <Banner/>
  )
}

export default Main
